import { Canvas, Meta, Story } from '@storybook/addon-docs/blocks';
import FileUploader from './FileUploader';

export const DefaultFileUploaderArgTypes = {
	title: {
		name: 'title',
		type: { name: 'string', required: false },
		description: 'Title of the file uploader.',
		control: {
			type: 'text',
		},
		defaultValue: 'Choose a file or drag it here',
	},
	listTitle: {
		name: 'listTitle',
		type: { name: 'string', required: false },
		description: 'Description of the files list ',
		control: {
			type: 'text',
		},
		defaultValue: 'Files',
	},
	submitButtonLabel: {
		name: 'submitButtonLabel',
		type: { name: 'string', required: false },
		description: 'Label of the submit button',
		control: {
			type: 'text',
		},
		defaultValue: 'Submit',
	},
	submitButtonCallback: {
		name: 'submitButtonCallback',
		type: { name: 'function', required: false },
		description: 'Callback function of the submit button',
		control: {
			type: 'function',
		},
		defaultValue: () => console.log('Submit button clicked'),
	},
	removeButtonLabel: {
		name: 'removeButtonLabel',
		type: { name: 'string', required: false },
		description: 'Label of the remove button',
		control: {
			type: 'text',
		},
		defaultValue: 'Remove all',
	},
};

<Meta title="MDX/FileUploader" component={FileUploader} argTypes={DefaultFileUploaderArgTypes} parameters={{
	controls: {
		expanded: true,
		actions: {
			handles: ['mouseover', 'click .btn'],
		},
	},
}}
/>

export const Template = args => {
	return <FileUploader {...args} />;
}

#### Playground

<Canvas>
	<Story name="Playground">
		{Template.bind({})}
	</Story>
</Canvas>

