import { Canvas, Meta, Story } from '@storybook/addon-docs/blocks';
import Calendar from './Calendar';

export const DefaultCalendarArgTypes = {
	displayWeekends: {
		name: 'displayWeekends',
		type: { name: 'boolean', required: false },
		description: 'Display weekends',
		control: {
			type: 'boolean',
		},
		defaultValue: true,
	},
	selectable: {
		name: 'selectable',
		type: { name: 'boolean', required: false },
		description: 'Allows a user to highlight multiple days or timeslots by clicking and dragging.',
		control: {
			type: 'boolean',
		},
		defaultValue: true,
	},
	editable: {
		name: 'editable',
		type: { name: 'boolean', required: false },
		description: 'Determines whether the events on the calendar can be modified.',
		control: {
			type: 'boolean',
		},
		defaultValue: true,
	},
	initialDate: {
		name: 'initialDate',
		type: { name: 'date', required: false },
		description: 'The initial date displayed when the calendar first loads. This value can be anything that can parse into a Date, "2014-02-01".',
		control: {
			type: 'date',
		}
	},
	buttonText: {
		name: 'buttonText',
		type: { name: 'object', required: false },
		description: 'Text that will be displayed on buttons of the headerToolbar/footerToolbar.',
		defaultValue: {
			today: 'Today',
			month: 'Month',
			week: 'Week',
			day: 'Day',
			list: 'List',
		},
	},
	buttonIcons: {
		name: 'buttonIcons',
		type: { name: 'object', required: false },
		description: 'Icons that will be displayed on buttons of the headerToolbar/footerToolbar. You can use icons from [Bootstrap](https://icons.getbootstrap.com/)',
		defaultValue: {
			prev: 'arrow-left',
			next: 'arrow-right',
			prevYear: 'left-double-arrow',
			nextYear: 'right-double-arrow',
		},
	},
	isSkeleton: {
		name: 'isSkeleton',
		type: { name: 'boolean', required: false },
		description: 'Display skeleton',
		control: {
			type: 'boolean',
		},
		defaultValue: false,
	},
}

<Meta title="MDX/Calendar" component={Calendar} argTypes={DefaultCalendarArgTypes} parameters={{
	controls: {
		expanded: true,
		actions: {
			handles: ['mouseover', 'click .btn'],
		},
	},
}}
/>

export const Template = args => {
	return <Calendar {...args} />;
}

# Default Calendar

Calendar was created using [FullCalendar](https://fullcalendar.io/).

<Canvas>
	<Story name="Default">
		{Template.bind({})}
	</Story>
</Canvas>

# Calendar Skeleton

<Canvas>
	<Story name="Skeleton" args={{ isSkeleton: true }}>
		{Template.bind({})}
	</Story>
</Canvas>

