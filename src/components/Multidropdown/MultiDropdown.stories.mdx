import { Canvas, Meta, Story } from '@storybook/addon-docs/blocks';
import { MultiDropdown } from './MultiDropdown.tsx';
import ExpandMoreIcon from '@mui/icons-material/ExpandMore';

export const textExample = `Lorem ipsum dolor sit amet. Est
blanditiis cumque ea suscipit ipsam est facilis nobis qui neque delectus in sint necessitatibus non nobis
quibusdam ut reiciendis quia. Vel sapiente officia eos modi neque ex atque possimus.`

export const ExampleItems = [
	{
		id: 1,
		value: 'Item 1',
	},
	{
		id: 2,
		value: 'Item 2',
		dropdownItems: [
			{
				id: 3,
				value: 'Sub-item 1',
				onClick: () => {},
				dropdownItems: [
					{
						id: 4,
						value: 'Sub-sub-item 1',
						onClick: () => {},
					},
					{
						id: 5,
						value: 'Sub-sub-item 2',
						onClick: () => {},
						dropdownItems: [
							{
								id: 6,
								value: 'Sub-sub-sub-item 1',
								onClick: () => {},
							},
							{
								id: 7,
								value: 'Sub-sub-sub-item 2',
								onClick: () => {},
							},
						],
					},
				],
			},
			{
				id: 8,
				value: 'Sub-item 2',
				onClick: () => {},
			},
		],
	},
	{
		id: 9,
		value: 'Item 3',
	},
]

export const DefaultDropdownArgTypes = {
	placeholder: {
		name: 'Placeholder',
		type: { name: 'string', required: false },
		defaultValue: 'Dropdown',
		description: 'Dropdown placeholder accepts string',
		table: {
			type: { summary: 'string' },
			defaultValue: { summary: 'Hello' },
		},
		control: {
			type: 'text'
		}
	},
	items: {
		name: 'items',
		description:
			"You can pass array with an objects see doc",
		defaultValue: ExampleItems,
	},
	icon: {
		name: 'Icon',
		description: 'The icon to display as the expand indicator. Accepts node element. To control element use class .FiveUi__dropdown-icon',
		defaultValue: <ExpandMoreIcon className="FiveUi__dropdown-icon"/>,
	},
	disabled: {
		name: 'Disabled',
		type: 'boolean',
		description: 'If true, the component is disabled.',
		defaultValue: false,
	},
	isExpanded: {
		name: 'IsExpanded',
		description: 'If true dropdown will be expanded by default',
		control: {
			type: 'boolean',
		},
		defaultValue: false,
	},
	position: {
		name: 'Position',
		description: 'Dropdown position',
		options: ['default', 'reversed'],
		control: {
			type: 'select',
		},
		mapping: {
			default: 'default',
			reversed: 'reversed',
		},
		defaultValue: 'default',
	},
	dropdownStyles: {
		name: 'DropdownStyles',
		description: 'Styles object that will be applied to dropdown.',
		defaultValue: {backgroundColor: '#fff', color: '#000'}
	},
	children: {
		name: 'Children',
		description: "Children element that will be display inside dropdown, DON'T pass if you passing 'items'",
	},
};

<Meta title="MDX/MultiDropdown" component={MultiDropdown} argTypes={DefaultDropdownArgTypes} parameters={{
	controls: {
		expanded: true,
		actions: {
			handles: ['mouseover', 'click .btn'],
		},
	},
	}}
/>

export const Template = args => {
	return <MultiDropdown {...args}  />;
}

#### Using items prop

<Canvas>
	<Story name="Using items prop" args={{ items: ExampleItems }}>
		{Template.bind({})}
	</Story>
</Canvas>

#### Using children prop

<Canvas>
	<Story name="Using children prop" args={{children: <div style={{padding: '30px'}}>{textExample}</div>}}>
		{Template.bind({})}
	</Story>
</Canvas>

#### Position reversed


<Canvas>
	<Story name="Reversed" args={{position: 'reversed'}} parameters={{layout: 'centered'}}>
		{Template.bind({})}
	</Story>
</Canvas>

